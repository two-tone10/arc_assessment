# Supabase project settings
# These are used by Vercel API routes. The anon key is public by design,
# but Supabase Row Level Security should be enabled before real pilots.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional AI insight generation
# Keep this secret in Vercel. Do not expose it in browser code.
OPENAI_API_KEY=

